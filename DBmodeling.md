# DB 모델링

## 모델링(Modeling)
- 모델을 만드는 과정

## 모델(Model)
- 실제 대상을 본떠 만든 것

## 시스템(System)
- 공통된 목적을 달성하기 위해 여러 요소들이 상호작용하는 구조

## 데이터 모델링(Data Modeling)
- 실제 업무를 분석하여 데이터 모델로 만드는 과정

---

# ER 모델의 질적 특성

ER 모델이 가져야 하는 특성

1. **완전성(Completeness)**  
   - 모든 업무 요구사항을 표현해야 한다.

2. **정확성(Correctness)**  
   - 현실 세계의 규칙과 의미를 올바르게 표현해야 한다.

3. **일관성(Consistency)**  
   - 같은 의미를 서로 다르게 표현하지 않아야 하며, 중복되거나 충돌되는 정의가 없어야 한다.

4. **최소성(Minimality)**  
   - 불필요한 개체, 속성, 관계를 줄여 간단하게 표현해야 한다.

5. **확장성(Flexibility / Extensibility)**  
   - 나중에 요구사항이 추가되어도 수정이 쉬워야 한다.

6. **이해 용이성(Understandability)**  
   - 사용자가 쉽게 이해할 수 있도록 구조가 명확해야 한다.

---

# ER 모델 구성 요소

- 엔티티(Entity)
- 관계(Relationship)
- 속성(Attribute)
- 식별자(Identifier)

---

# 엔티티(Entity)

- 실존하거나 개념적인 대상

### 예시
- 실존하는 것: 고객, 상품, 직원
- 개념적인 것: 주문, 결제

---

# 엔티티 간의 관계

- 엔티티 사이의 연관성을 표현
- 관계수(Cardinality), 선택성(Optionality), 식별자 상속 등을 포함

## 관계 유형
- 기본 관계
- 재귀적 관계
- 병렬 관계
- 슈퍼타입 / 서브타입 관계

---

# 데이터 모델링

- 실제 업무를 분석하여 데이터 모델로 만드는 작업

## 관련 개념

### 모델(Model)
- 작품을 만들기 전에 미리 만든 예시나 형태

### 모델링(Modeling)
- 모델을 만드는 과정

### 데이터 모델(Data Model)
- 데이터를 그룹화하고 그룹 간의 관계를 표현한 것  
- 대표적으로 ER 모델(Entity-Relationship Model)이 있다.

---

# 데이터 모델링 과정

현행 업무를 파악한 후 다음 자료들을 분석한다.

- 현업 인터뷰
- 업무 지침서
- 용어집
- 산출물

이후 개념들을 정의하고 분류하여 엔티티(Entity), 속성(Attribute), 관계(Relationship) 형태로 표현한다.