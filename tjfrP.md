# DB 모델링

### 모델링 : 모델 만들기

### 모델 : 진짜를 본 떠서 만드는것

### 시스템 : 공통된 목적을 달성하기 위해 작용하는 여러 요소들

### 데이터 모델링 : 실제 업무를 분석하여 데이터 모겔로 만드는 것

### ER 모델의 질적 특성
- 1. 완전성(Completeness) : 시스템에 필요한 데이터와 관계를 빠짐없이 표현해야 함
- 2. 정확성(Correctness) : 현실 세계의 규칙과 의미를 올바르게 표현해야 함
- 3. 일관성(Consistency) : 같은 의미를 서로 다르게 표현하지 않아야 함, 중복되거나 충돌되는 정의가 없어야 함
- 4. 최소성(Minimality) : 불필요한 개체, 속성, 관계를 줄여 간단하게 표현해야 함
- 5. 확장성(Flexibility / Extensibility) : 나중에 요구사항이 추가되어도 수정이 쉬워야 함
- 6. 이해 용이성(Understandability) : 사용자가 쉽게 이해할 수 있게 구조가 명확해야 함

### ER 모델 구성요소
- 엔티티
- 관계
- 속성
- 식별자

### 엔티티간의 관계
- 엔티티간의 관계를 표현 - 관계수, 선택상, 식별자 상속
